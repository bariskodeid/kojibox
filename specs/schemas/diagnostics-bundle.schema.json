{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Kojibox Diagnostics Bundle",
  "type": "object",
  "required": ["path", "createdAt", "includes"],
  "properties": {
    "path": {"type": "string"},
    "createdAt": {"type": "string", "format": "date-time"},
    "includes": {"type": "array", "items": {"type": "string"}}
  }
}
